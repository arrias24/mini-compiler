#ifndef TYPE_TOKEN_H
#define TYPE_TOKEN_H

enum class TokenType {
    KEYWORD,
    IDENTIFIER,
    OPERATOR,
    ASSIGNMENT,
    VALUE,
    OPEN_DELIMITER,
    CLOSE_DELIMITER,
    DELIMITER,
    TEXT_DELIMITER,
    UNKNOWN
};

#endif